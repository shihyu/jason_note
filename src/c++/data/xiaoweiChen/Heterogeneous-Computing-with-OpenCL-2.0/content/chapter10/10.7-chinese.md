#10.7 使用`printf`調試

OpenCL C內核語言也實現了C語言中著名的printf()函數。printf()函數也可以用來進行調試，這種調試方式對於C/C++開發者來說並不陌生。

當內核調用完成時，設備會執行所有調用的printf()對輸出流進行刷新。對命令隊列使用clFinish()能將所有入隊內核中的printf()進行執行，打印出對應變量的信息。這裡需要注意的是，和多線程程序一樣，printf()打印出來的信息並不是順序的。

OpenCL-C標準文檔中說明，OpenCL-C與C99標準的實現由所不同。