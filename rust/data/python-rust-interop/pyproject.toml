[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "rust_python_example"
version = "0.1.0"
description = "A simple example of Rust-Python interoperability"
requires-python = ">=3.7"

[tool.maturin]
features = ["pyo3/extension-module"]