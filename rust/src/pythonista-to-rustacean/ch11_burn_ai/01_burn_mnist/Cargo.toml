[package]
name = "burn_mnist"
version = "0.1.0"
edition = "2024"

[dependencies]
burn = { version = "0.18.0", default-features = false, features = ["train", "vision", "ndarray", "wgpu", "std", "tui", "fusion"] }
